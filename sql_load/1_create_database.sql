-- Create a new database named 'sql_course'. This command initializes the database and prepares it for use.
CREATE DATABASE sql_course;


-- Delete the "--" if you want to drop the database. This command will remove the 'sql_course' database and all its contents permanently.
-- DROP DATABASE sql_course;